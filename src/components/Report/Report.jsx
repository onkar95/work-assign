import React from 'react'

const Report = ({ }) => {

    return (
        <div>
            {data && data.map((val, index) => (
                <div className='A4-Page'>

                </div>
            ))}
        </div>
    )
}

export default Report